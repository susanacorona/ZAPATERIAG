{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo Almacen</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Guardar registro" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('almacen_index') }}">Almacen principal</a>
        </li>
    </ul>
{% endblock %}
